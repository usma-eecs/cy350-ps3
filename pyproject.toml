[project]
name = "cy350-ps3-telemetry"
version = "0.1.0"
description = "Rocket telemetry parsing assignment for CY350"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["main"]
